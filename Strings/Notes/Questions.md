Basic String Handling (1–15)

Input and print a string.

Find the length of a string.

Convert a string to uppercase.

Convert a string to lowercase.

Compare two strings (equals).

Compare two strings (ignoring case).

Concatenate two strings.

Check if a string is empty.

Check if a string contains a particular character.

Check if a string starts with a given prefix.

Check if a string ends with a given suffix.

Find the first occurrence of a character in a string.

Find the last occurrence of a character.

Get a substring from a string.

Replace all occurrences of a character in a string.

✅ Character-Level Operations (16–30)

Count vowels and consonants in a string.

Count digits, alphabets, and special characters.

Count the frequency of each character.

Remove all vowels from a string.

Remove all spaces from a string.

Remove all digits from a string.

Reverse a string.

Reverse each word in a string.

Check if a string is a palindrome.

Toggle case of each character.

Count uppercase and lowercase letters.

Remove duplicate characters from a string.

Check if all characters are unique.

Find the character with the highest frequency.

Replace spaces with hyphens or underscores.

✅ Intermediate String Logic (31–50)

Check if two strings are anagrams.

Print all substrings of a string.

Find the longest word in a string.

Print duplicate characters in a string.

Remove all occurrences of a given character.

Remove the first occurrence of a character.

Capitalize the first letter of each word.

Swap two words in a string.

Count number of words in a string.

Count occurrences of each word in a sentence.

Remove extra spaces from a sentence.

Sort characters in a string alphabetically.

Sort words in a sentence.

Check if string is a valid identifier (Java variable name).

Compress the string (e.g., aaabb → a3b2).

Convert a sentence to title case.

Find the longest palindromic substring.

Find the second most frequent character.

Convert a character array to a string.

Convert a string to a character array.

✅ String Pattern and Matching (51–65)

Find if a pattern exists in a string.

Find index of substring.

Implement custom logic for contains() function.

Implement your own equalsIgnoreCase() method.

Replace a word in a sentence.

Find the first repeated word.

Find the last repeated word.

Remove all repeated words.

Check if a string is a pangram.

Find smallest and largest word in a string.

Replace each word with its reverse.

Insert a word at a specific position in a string.

Remove numeric words from a sentence.

Remove punctuation from a string.

Split a string based on delimiter.

✅ Advanced String Problems (66–75)

Check if a string has balanced parentheses.

Check if a string is a rotation of another.

Find longest common prefix among an array of strings.

Find longest common suffix among an array of strings.

Encode a string using Run Length Encoding (RLE).

Implement Caesar Cipher Encryption.

Decode a Caesar Cipher encrypted string.

Find minimum number of deletions to make a string palindrome.

Print all permutations of a string.

Implement pattern matching using matches() and regex.