public class PrintASCIIValue {
    public static void main(String[] args) {
        char ch = 'a';
        // No need of typeCasting because we are converting smaller datatype into larger datatype.
        int ASCII = ch;
        System.out.println(ASCII);
    }
}
